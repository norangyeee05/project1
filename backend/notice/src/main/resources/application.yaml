spring:
  application:
    name: notice
  datasource:
    url: jdbc:postgresql://localhost:5432/business
    username: postgres
    password: 1004
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
      show-sql: false
server:
  port: 8081